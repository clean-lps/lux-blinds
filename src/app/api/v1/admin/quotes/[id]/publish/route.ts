import{NextResponse}from'next/server';import{requireActor}from'@/server/auth/session';import{publishQuote}from'@/server/admin/orders';export async function POST(r:Request,c:{params:Promise<{id:string}>}){const requestId=crypto.randomUUID();try{const b=await r.json();return NextResponse.json({data:await publishQuote(await requireActor(r),(await c.params).id,b.expectedVersion),requestId});}catch{return NextResponse.json({error:{code:'FORBIDDEN',message:'Unable to publish quote',retryable:false},requestId},{status:403});}}
