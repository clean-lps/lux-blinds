import{NextResponse}from'next/server';import{requireActor}from'@/server/auth/session';import{listAudit}from'@/server/admin/orders';export async function GET(r:Request,c:{params:Promise<{id:string}>}){const requestId=crypto.randomUUID();try{return NextResponse.json({data:await listAudit(await requireActor(r),(await c.params).id),page:{nextCursor:null},requestId});}catch{return NextResponse.json({error:{code:'FORBIDDEN',message:'Forbidden',retryable:false},requestId},{status:403});}}
