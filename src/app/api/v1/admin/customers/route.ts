import {NextResponse} from 'next/server';import {requireActor} from '@/server/auth/session';import {listCustomers} from '@/server/admin/customers';export async function GET(r:Request){const requestId=crypto.randomUUID();try{return NextResponse.json({data:await listCustomers(await requireActor(r)),page:{nextCursor:null},requestId});}catch{return NextResponse.json({error:{code:'FORBIDDEN',message:'Forbidden',retryable:false},requestId},{status:403});}}
