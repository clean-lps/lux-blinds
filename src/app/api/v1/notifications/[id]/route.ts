import{NextResponse}from'next/server';import{requireActor}from'@/server/auth/session';import{markNotificationRead}from'@/server/notifications/service';export async function PATCH(r:Request,c:{params:Promise<{id:string}>}){const requestId=crypto.randomUUID();try{return NextResponse.json({data:await markNotificationRead(await requireActor(r),(await c.params).id),requestId});}catch{return NextResponse.json({error:{code:'NOT_FOUND',message:'Not found',retryable:false},requestId},{status:404});}}
