import{NextResponse}from'next/server';import{requireActor}from'@/server/auth/session';import{listNotifications}from'@/server/notifications/service';export async function GET(r:Request){const requestId=crypto.randomUUID();try{return NextResponse.json({...await listNotifications(await requireActor(r),{unread:new URL(r.url).searchParams.get('unread')==='true'}),requestId});}catch{return NextResponse.json({error:{code:'UNAUTHORIZED',message:'Authentication required',retryable:false},requestId},{status:401});}}
